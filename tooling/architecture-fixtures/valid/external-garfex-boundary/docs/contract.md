# External contract

TypeSpec is the sole external semantic authority. The trusted edge maps exactly ten named operations to the Resource Master public application contract, which performs final authorization. This document is canonical architecture evidence; the manifest and generated contract are downstream artifacts.
